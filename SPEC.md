# SPEC.md — hyakunin-lens(百人レンズ)

<!-- scaffold template v1.8.0 から展開(2026-08-20)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

AI研究者・エージェント実装者 100 名について、各人の紹介文と ①本人の発信(ブログ / Substack / 研究所ブログ / X / LinkedIn)②インタビュー・ポッドキャスト・動画・講演 ③YouTube 対談動画 を最大 3 件ずつ一覧できる単一 HTML ダッシュボードを提供する。データ(2026-08-20 収集・746 件)は `data/*.json` に正規化して保持し、ビルド器が静的ページ `out/index.html` を再現可能に生成する。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | `data/people.json` に 100 名分のレコード(n/c/h/bio/own/med/yt、任意で note/mnote/ynote)を保持する | must |
| F-02 | `src/build.py` が data + `src/template.html` から自己完結の `out/index.html` を生成する(外部リソース参照なし) | must |
| F-03 | ページはカテゴリ絞り込み(11 分類)・全文検索・セクション切替(本人/メディア/YouTube)・「直近1ヶ月」「直近1週間」絞り込み(閲覧時点基準・排他)を提供する。セクション切替と期間フィルタのボタンは系統色(本人=緑/メディア=橙/YouTube=赤/期間=シアン)を持つ | must |
| F-04 | 件数行(100名 · 237/263/246 · 計746件)はデータから算出して埋め込む(手書きの数字を持たない) | must |
| F-05 | 日付「不明」の項目は淡色表示し、期間絞り込みの対象外とする(サンプル準拠) | must |
| F-06 | `data/sources.json` に人物ごとのフィード取得経路(blog / substack / lab / note の RSS・Atom)を宣言的に保持する(構造的に使用不能なフィードは skip/skip_reason で無効化)。X・インタビュー・bio は月次 routine、YouTube は F-10、LinkedIn のみ初回収集のまま | must |
| F-07 | `src/update.py` が宣言フィードを収集し、成功したソース種別の項目だけを差し替えて own を再構成する(他種別・失敗時は既存維持=劣化継続)。各セクション最大 3 件・日付降順(不明は末尾)。更新後に out/index.html を再生成する | must |
| F-08 | GitHub Actions cron(6 時間間隔)が update を実行し、差分があるときだけ data/ + out/ をコミットする(Vercel Git 連携で自動デプロイ) | must |
| F-09 | フッタは koho-lens と同構成(MIT License © ・ GitHub ・ 歩き方 ・ 設計図 ・ App Menu の単一段落)で常に画面最下部に固定表示。ヘッダーに最終更新時刻(JST)と自動更新範囲を表示する | must |
| F-10 | YouTube 対談セクションを YouTube Data API v3 の検索で自動更新する。誤帰属ゲート(動画タイトルに氏名の語形が含まれること)、own/med と重複する URL の除外、劣化継続、日次 50 名ローテーション(2 日で一巡・無料クォータの 50% 以内)。API キー未設定時は無変更で正常終了 | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | ビルドは決定的(同じ data から同じバイト列) | T-05 |
| N-02 | 全テキストは HTML エスケープして埋め込む(XSS/表示崩れ防止) | T-04 |
| N-03 | Vercel に `out/` をそのまま静的配信(buildCommand なし) | vercel.json |

## 4. 品質基準

- 人物 100 名・項目合計 746 件(own 237 / med 263 / yt 246)がデータ検証テストで固定される
- ビルド出力から埋め込み JSON を再抽出すると `data/*.json` と完全一致する(往復一致オラクル)
- 日付は `YYYY-MM-DD` / `YYYY-MM` / `不明` のみ。URL は http(s) のみ。カテゴリ・ソース種別は語彙表に閉じる

## 5. スコープ外

- X / LinkedIn のライブ取得(不可能または規約上塞がれている)。これらの項目は 2026-08-20 スナップショットを維持し、月次のエージェント再収集(routine)で補完する
- フィード新着タイトルの和訳(自動収集分は原文タイトルのまま掲載し、その旨を「データの制約」に明記)
- 紹介文(bio)・インタビュー(med)セクションのスクリプト自動更新(一般 Web の機械収集は誤帰属リスクが高い。こちらも月次 routine の対象)
